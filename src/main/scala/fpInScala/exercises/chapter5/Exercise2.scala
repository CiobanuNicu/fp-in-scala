package fpInScala.exercises.chapter5

object Exercise2 {
  // Write the function take(n) for returning the first n elements of a Stream, and drop(n) for skipping the first n
  // elements of a Stream.
}
